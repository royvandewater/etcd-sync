package local

import (
	"errors"

	"github.com/royvandewater/etcdsync/heirarchy"
)

// GenerateHeirarchy generates a heirarchy from the local filesystem
func GenerateHeirarchy(path string) (*heirarchy.Heirarchy, error) {
	return nil, errors.New("uh oh")
}
